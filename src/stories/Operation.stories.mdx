import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Operation from '../components/Operation.tsx';

<Meta title="Components/Operation" component={Operation} />

# Operation

Operation between two numbers

export const Template = (args) => <Operation {...args} />;

<Canvas>
    <Story name="Sum">
        { Template.bind({}) }
    </Story>
</Canvas>